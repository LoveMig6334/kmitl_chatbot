[project]
name = "thai-llm-kmitl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Thatt Bunnag", email = "tom.tom.thanet@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
]

[project.scripts]
thai-llm-kmitl = "thai_llm_kmitl:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.4",
]
